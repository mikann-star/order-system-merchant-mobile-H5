# 商家端移动端－修改登录密码 PRD

| 页面/一级模块 | 二级模块 | 需求内容 |
| --- | --- | --- |
| 账户设置 | 登录密码－状态展示 | 1. 在「账号设置」的「登录安全」区域展示登录密码条目。<br>2. 左侧展示锁形图标；中间上方展示标题「登录密码」，下方展示灰色辅助文字「上次修改：YYYY-MM-DD」。<br>3. 右侧展示「修改密码」按钮。<br>4. 点击「修改密码」打开「修改登录密码」弹窗。 |
| 修改登录密码弹窗 | 弹窗结构与遮罩 | 1. 弹窗以底部弹窗形式展示，外层展示半透明遮罩。<br>2. 遮罩展示期间不可操作背景页面。<br>3. 弹窗自上而下包含顶部栏、内容区、操作栏。 |
| 修改登录密码弹窗 | 顶部栏 | 1. 左侧展示标题「修改登录密码」。<br>2. 右侧展示「×」关闭按钮。<br>3. 点击「×」关闭弹窗，清空原密码、新密码、确认新密码及校验提示，不修改密码。 |
| 修改登录密码弹窗 | 原密码输入 | 1. 展示字段标题「原密码」。<br>2. 输入框类型为密码输入框，输入内容默认隐藏；占位文案为「请输入原密码」。<br>3. 原密码用于身份核验，不参与新密码规则校验。<br>4. 用户重新输入原密码后，需清除该字段已有错误提示。 |
| 修改登录密码弹窗 | 新密码输入 | 1. 展示字段标题「新密码」。<br>2. 输入框类型为密码输入框，输入内容默认隐藏；占位文案为「请输入新密码」。<br>3. 用户输入时实时校验长度与字符组合规则。<br>4. 输入框下方固定展示密码规则，校验标识随输入状态更新。 |
| 修改登录密码弹窗 | 新密码规则 | 1. 长度限制：8-20 位字符。<br>2. 组合要求：大写字母、小写字母、数字、常用特殊符号（!@#$%^&*_）中，至少包含三类。<br>3. 单条规则满足时，规则前展示绿色「✓」；未满足时展示灰色空心圆「○」。<br>4. 两项规则均满足时，视为新密码合规。 |
| 修改登录密码弹窗 | 确认新密码输入 | 1. 展示字段标题「确认新密码」。<br>2. 输入框类型为密码输入框，输入内容默认隐藏；占位文案为「请再次输入新密码」。<br>3. 输入过程中实时校验是否与新密码完全一致。<br>4. 两次密码不一致时，在确认新密码输入框下方展示红色提示「两次输入的密码不一致」。<br>5. 两次密码一致但新密码未满足基础规则时，展示红色提示「请输入符合要求的新密码」。<br>6. 两次密码一致且新密码合规时，确认密码校验通过。 |
| 修改登录密码弹窗 | 操作栏 | 1. 底部横向展示「取消」与「确定修改」按钮。<br>2. 「取消」为次按钮；点击后关闭弹窗并清空本次输入及校验状态，不修改密码。<br>3. 「确定修改」为主按钮。 |
| 修改登录密码弹窗 | 确定修改按钮状态 | 1. 可点击条件：新密码满足全部基础规则，且确认新密码与新密码完全一致。<br>2. 未满足任一条件时，按钮置灰且不可点击。<br>3. 当前原型在按钮禁用时通过置灰状态表达不可提交；正式版本可补充辅助提示「请先输入符合规则的新密码」。 |
| 修改登录密码弹窗 | 提交校验与成功反馈 | 1. 点击「确定修改」后，系统校验原密码正确性。<br>2. 原密码校验失败：原密码输入框进入错误状态，展示红色提示「原密码错误，请重新输入」；弹窗不关闭，用户可重新输入后提交。<br>3. 原密码校验成功：更新登录密码，关闭弹窗；页面顶部展示绿色 Toast「密码修改成功」，持续 1 秒后自动消失。<br>4. 成功后更新「上次修改」日期。 |
| 修改登录密码弹窗 | 数据与安全约束 | 1. 密码输入内容不得以明文展示或记录在前端日志中。<br>2. 本期原型使用演示账号原密码进行校验；接入服务端后，应由服务端完成原密码校验、新密码合规校验与密码更新。<br>3. 接口字段至少包括：账号 ID、原密码、新密码、确认新密码、修改结果及失败原因。 |
