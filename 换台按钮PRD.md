# 移动端换台按钮 PRD

| **页面/一级模块** | **二级模块** | **需求内容** |
| --- | --- | --- |
| 桌台详情页 | 换台按钮展示 | 1. 【换台】位于「快捷操作」区域。<br>2. 仅当桌台状态为【就餐中】时可点击。<br>3. 桌台状态为【空闲】或【待清理】时，按钮置灰并不可点击。 |
| 桌台详情页 | 换台入口 | 当前桌台为【就餐中】时，点击【换台】按钮，打开从页面底部弹出的「选择目标桌台」抽屉；抽屉展示遮罩层，屏蔽背景区域点击与滚动。 |
| 选择目标桌台弹窗 | 标题与关闭 | 1. 标题位于左上角：`选择目标桌台`。<br>2. 标题下方展示当前就餐人数：`当前就餐人数：X人`。<br>3. 右上角展示「×」关闭按钮；点击后关闭弹窗并返回当前桌台详情页。 |
| 选择目标桌台弹窗 | 可用桌台列表 | 1. 仅展示状态为【空闲】的桌台。<br>2. 使用移动端三列自适应网格。<br>3. 弹窗最大高度约为屏幕高度的 82%；空闲桌台数量超出当前可视区域时，列表区域支持上下滑动查看全部桌台。<br>4. 单个桌台卡片展示：`{桌号}号桌`、`{容纳人数}人`。<br>5. 无空闲桌台时，居中展示：`暂无可用桌台`。 |
| 选择目标桌台弹窗 | 桌台选择交互 | 1. 桌台卡片的完整区域可点击。<br>2. 点击或按压卡片时，边框变为蓝色并展示浅蓝底色。<br>3. 点击后仅选中目标桌台卡片，不弹出二次确认弹窗。<br>4. 同一时间仅允许选中一个目标桌台。 |
| 选择目标桌台弹窗 | 底部操作按钮 | 1. 固定展示【取消】、【确认换台】按钮。<br>2. 未选择目标桌台时，【确认换台】置灰不可点击。<br>3. 【取消】：关闭抽屉，返回当前桌台详情页。<br>4. 【确认换台】：直接执行容量校验与换台逻辑。 |
| 选择目标桌台弹窗 | 确认换台与校验 | 1. 点击【确认换台】时校验：当前桌台实际就餐人数 ≤ 目标桌台容纳人数。<br>2. 校验失败：页面中心展示 `目标桌台容纳人数不足`，约 1 秒后自动消失；保留在当前选择目标桌台抽屉，不执行换台。<br>3. 校验通过：原桌台释放为【空闲】；目标桌台变更为【就餐中】；目标桌台继承原桌台的开台时间与就餐人数。<br>4. 关闭抽屉并返回桌台管理页，展示换台成功提示。 |
