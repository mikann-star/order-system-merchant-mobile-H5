# 结账按钮（移动端）

## 2. 【结账】按钮

1. 功能：针对已下单区域所有有效、未退款菜品发起结账支付，结账金额为合计总金额。

2. 可点击场景：
   1. 待下单区域无菜品且合计总金额大于等于 ¥0.00。
   2. 移动端不使用置灰禁用样式；在不满足上述条件时，按钮仍可点击，并通过居中悬浮提示反馈原因。

3. 不可结账场景及移动端交互：
   1. 待下单区域有菜品：点击后在界面中心展示悬浮提示“待下单区域存在未处理菜品，请先提交下单或删除待下单菜品后，再进行结账。”
   2. 合计总金额小于 ¥0.00：点击后在界面中心展示悬浮提示“当前待付金额为负数，请调整折扣后再结账。”
   3. 待下单区域有菜品且合计总金额小于 ¥0.00：优先展示“待下单区域存在未处理菜品，请先提交下单或删除待下单菜品后，再进行结账。”
   4. 悬浮提示展示约 1 秒后自动消失，不弹出二次确认弹窗。

4. 点击确认弹窗：
   1. 仅在满足可点击场景时弹出二次确认弹窗。
   2. 弹窗标题：暂不设置标题。
   3. 弹窗文案：橙色感叹号图标 + “是否确定对当前桌台进行结账，当前消费金额为 ¥{合计总金额}？”。

5. 弹窗交互：
   1. 【确定】：完成结账，桌台状态由【就餐中】变更为【待清理】。
   2. 【取消】：关闭弹窗，返回点餐管理页面，不执行任何操作。

## 当前原型说明

当前原型暂未设置“待下单区域”，因此待下单菜品数量默认视为 0。后续接入待下单数据后，需将其数量关联至结账条件。
