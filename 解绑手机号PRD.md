# 商家端移动端－解绑手机号 PRD

| 页面/一级模块 | 二级模块 | 需求内容 |
| --- | --- | --- |
| 账户设置 | 手机号－解绑入口 | 1. 前置条件：当前账号已绑定手机号。<br>2. 在「账号设置 · 绑定信息」中，手机号条目左侧展示手机图标；中间展示「手机号」及当前绑定手机号的脱敏信息；右侧并排展示「更换」与「解绑」按钮。<br>3. 点击「解绑」按钮，打开「解绑手机号」确认弹窗。 |
| 解绑手机号弹窗 | 弹窗结构与遮罩 | 1. 弹窗以底部弹窗形式展示，弹窗外层展示半透明遮罩。<br>2. 遮罩展示期间不可操作页面背景内容。<br>3. 弹窗自上而下包含顶部栏、内容区、操作栏。 |
| 解绑手机号弹窗 | 顶部栏 | 1. 左侧展示弹窗标题「解绑手机号」。<br>2. 右侧展示「×」关闭按钮。<br>3. 点击「×」关闭弹窗，清空本次已输入验证码及错误提示，不执行解绑，不修改手机号绑定状态。 |
| 解绑手机号弹窗 | 当前手机号信息 | 1. 内容区展示字段标题「当前手机号」。<br>2. 下方展示当前已绑定手机号的脱敏信息，输入框为只读状态，不允许编辑。<br>3. 展示格式为「+{区号} {脱敏后手机号}」，例如「+86 138****8000」。<br>4. 脱敏规则：手机号中间数字使用「*」隐藏；隐藏位数为手机号总位数的二分之一，向下取整。 |
| 解绑手机号弹窗 | 验证码输入 | 1. 展示字段标题「验证码」。<br>2. 左侧展示 6 位数字验证码输入框，占位文案为「请输入 6 位验证码」。<br>3. 输入内容仅允许数字，最大长度为 6 位。<br>4. 右侧展示「获取验证码」按钮。 |
| 解绑手机号弹窗 | 获取验证码 | 1. 点击「获取验证码」后，向当前绑定手机号发送解绑验证码。<br>2. 按钮进入 60 秒倒计时置灰状态，格式为「{N}s 后重试」；倒计时期间不可重复点击。<br>3. 倒计时结束后，按钮恢复为「获取验证码」。<br>4. 验证码有效期为 10 分钟。<br>5. 短信模板：`【FKM 点餐系统】您正在进行手机号解绑操作，验证码为 {6位数字验证码}，10分钟内有效，请勿泄露给他人。如非本人操作请立即检查账户安全。`<br>6. 当前原型使用演示验证码 `123456` 进行校验；接入服务端后以服务端返回结果为准。 |
| 解绑手机号弹窗 | 操作栏 | 1. 弹窗底部横向展示「取消」与「确认解绑」按钮。<br>2. 「取消」为次按钮；点击后关闭弹窗、清空本次验证码及错误提示，不执行解绑。<br>3. 「确认解绑」为主按钮；点击后校验验证码。 |
| 解绑手机号弹窗 | 解绑校验与成功反馈 | 1. 验证码正确时，执行解绑操作。<br>2. 解绑成功后关闭弹窗，页面展示成功 Toast：「手机号解绑成功」，持续 1 秒后自动消失。<br>3. 手机号条目实时刷新为未绑定状态：中间展示「未绑定」，右侧操作入口由「更换／解绑」切换为「去绑定」。 |
| 解绑手机号弹窗 | 校验失败与异常提示 | 1. 验证码为空、位数不足或校验失败时，不关闭弹窗，不修改原绑定手机号。<br>2. 验证码输入框进入错误状态，并展示明确错误提示；示例：「验证码不正确，请重新输入」「验证码已过期，请重新获取」。<br>3. 用户可修改验证码后再次提交，或重新获取验证码。 |
| 解绑手机号弹窗 | 数据与安全约束 | 1. 解绑仅影响当前账号的手机号绑定关系，不影响其他账户信息。<br>2. 解绑结果需与账户设置页绑定状态、操作按钮及密码找回可用验证方式保持一致。<br>3. 接口字段至少包括：账号 ID、原手机号、区号、验证码、验证码发送时间、解绑结果及失败原因。 |
