# 商家端移动端 PRD｜更换手机号

| 页面/一级模块 | 二级模块 | 需求内容 |
| --- | --- | --- |
| 账号设置 | 手机号－更换入口 | 1. 前置条件：当前账号已绑定手机号。<br>2. 在「账号设置 · 绑定信息」中，手机号条目展示手机图标、标题「手机号」及脱敏后的当前手机号；右侧展示「更换」主按钮和「解绑」次按钮。<br>3. 点击「更换」后，打开「更换手机号」弹窗。 |
| 更换手机号弹窗 | 弹窗结构 | 1. 弹窗展示半透明遮罩，遮罩存在时不可操作页面背景。<br>2. 弹窗由顶部栏、内容区、底部操作栏组成。<br>3. 顶部栏左侧标题为「更换手机号」，右侧为「×」关闭按钮。<br>4. 点击「×」或任一步「取消」均关闭弹窗，清空本次流程中的手机号、验证码、错误提示及倒计时状态，不修改已绑定手机号。 |
| 更换手机号弹窗 | 流程控制 | 1. 更换流程分为两步：「验证原手机号身份」和「绑定新手机号」。<br>2. 弹窗首次打开时进入第一步；仅当第一步身份验证通过后，才可进入第二步。<br>3. 第一步通过后清空原手机号验证码输入及错误提示；新手机号、区号和新手机号验证码在第二步内填写。 |
| 第一步－原手机号展示 | 当前手机号 | 1. 展示步骤说明「验证原手机号身份」。<br>2. 展示字段「当前手机号」，内容只读不可编辑。<br>3. 展示格式为「+{区号} {脱敏后手机号}」，例如「+86 138****8000」。<br>4. 脱敏规则：隐藏手机号中部数字，隐藏位数为号码总长度的二分之一并向下取整。 |
| 第一步－验证码 | 获取与输入 | 1. 展示「验证码」输入框及「获取验证码」按钮；输入框占位文案为「请输入 6 位验证码」。<br>2. 验证码只允许输入数字，最多 6 位。<br>3. 点击「获取验证码」后，向原绑定手机号发送身份验证短信，按钮进入 60 秒倒计时置灰状态，文案为「{N}s 后重试」；倒计时期间不得重复发送。<br>4. 验证码有效期为 10 分钟。<br>5. 短信内容：`【FKM 点餐系统】您正在进行手机号更换操作，本次身份验证验证码为 {6位数字验证码}，10分钟内有效，请勿泄露给他人。如非本人操作请立即检查账户安全。`<br>6. 当前前端演示环境以 `123456` 作为正确验证码；接入服务端后以服务端校验结果为准。 |
| 第一步－操作 | 下一步校验 | 1. 操作栏展示「取消」和「下一步」按钮。<br>2. 点击「下一步」时校验原手机号验证码。<br>3. 校验通过后进入第二步。<br>4. 验证码为空、位数不足、错误或过期时，停留在第一步，验证码输入框进入错误状态；展示对应提示，例如「验证码不正确，请重新输入」或「验证码已过期，请重新获取」。 |
| 第二步－新手机号 | 区号与号码输入 | 1. 展示步骤说明「绑定新手机号」。<br>2. 展示字段「新手机号」；输入区由区号选择器和号码输入框组成，号码输入框占位文案为「请输入手机号」。<br>3. 区号默认使用原绑定手机号的区号；当前实现默认展示 `+86`。支持选择：中国大陆 `+86`、中国香港 `+852`、新加坡 `+65`。<br>4. 输入仅保留数字；失焦时执行格式校验。校验失败时输入框进入错误状态，并提示「请输入正确的手机号」。<br>5. 格式规则：`+86` 为以 1 开头、第二位为 3–9 的 11 位数字；`+852` 为以 5、6、8、9 开头的 8 位数字；`+65` 为以 8、9 开头的 8 位数字。 |
| 第二步－验证码 | 获取与输入 | 1. 展示「验证码」输入框及「获取验证码」按钮；验证码只允许输入数字，最多 6 位。<br>2. 仅当新手机号通过格式校验后，「获取验证码」按钮可用；未通过时按钮置灰，并提示用户先输入正确的手机号。<br>3. 点击按钮后，向新手机号发送验证码；按钮进入 60 秒倒计时置灰状态，期间不可重复发送。<br>4. 验证码有效期为 10 分钟。<br>5. 短信内容：`【FKM 点餐系统】您正在绑定新手机号，本次验证码为 {6位数字验证码}，10分钟内有效，请勿泄露给他人。如非本人操作请忽略。`<br>6. 当前前端演示环境以 `123456` 作为正确验证码；接入服务端后以服务端校验结果为准。 |
| 第二步－操作 | 确定更换 | 1. 操作栏展示「取消」和「确定更换」主按钮。<br>2. 点击「确定更换」后，先校验新手机号格式，再校验新手机号验证码。<br>3. 校验成功后，更新当前账号的绑定手机号，关闭弹窗，并刷新账号设置页中的脱敏手机号信息。<br>4. 成功后展示绿色 Toast「手机号绑定成功」，展示 1 秒后自动消失；如产品需准确表达操作目的，可统一调整为「手机号更换成功」。<br>5. 新手机号格式不合法时提示「请输入正确的手机号」；验证码错误或过期时保留当前内容并展示对应错误提示，不关闭弹窗，也不修改原手机号。 |
| 通用约束 | 数据与安全 | 1. 更换操作仅更新当前账号的手机号绑定关系。<br>2. 取消、关闭、验证失败或验证码过期均不得改变原手机号。<br>3. 倒计时期间须防止重复发送验证码。<br>4. 服务端接口应至少关联账号 ID、原手机号与区号、新手机号与区号、验证码、验证码发送时间、校验结果及失败原因。 |
