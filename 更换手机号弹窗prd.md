# 商家端移动端 PRD｜更换手机号弹窗

| 页面/一级模块 | 二级模块 | 需求内容 |
| ------- | ---- | ---- |
| 我的 | 账号绑定信息－手机号 | 1. 手机号处于已绑定状态时，右侧展示「更换」主按钮与「解绑」次按钮。<br>2. 点击「更换」按钮后，打开「更换手机号」底部弹窗。 |
| 更换手机号弹窗 | 展示形式 | 1. 弹窗从页面底部弹出，顶部圆角；弹窗外展示半透明遮罩。<br>2. 弹窗展示期间不可操作背景页面，内容超过屏幕高度时弹窗内容区可纵向滚动。<br>3. 弹窗整体采用顶部栏、内容区、底部操作区的三段式结构。 |
| 更换手机号弹窗 | 顶部栏 | 1. 左侧展示标题「更换手机号」。<br>2. 右侧展示「×」关闭按钮。<br>3. 点击「×」后关闭弹窗，清空当前流程中已输入的验证码、新手机号等信息，不修改原绑定手机号。 |
| 更换手机号弹窗 | 流程规则 | 1. 更换流程分为两步：第一步「原手机号身份验证」、第二步「绑定新手机号」。<br>2. 用户必须完成第一步验证后，才可进入第二步。<br>3. 关闭弹窗或点击任一步「取消」均终止流程，原手机号保持不变。 |
| 更换手机号弹窗 | 第一步－原手机号展示 | 1. 内容区展示步骤说明「验证原手机号身份」。<br>2. 展示当前绑定手机号脱敏信息，字段只读不可编辑。<br>3. 展示格式为「+{区号} {脱敏后手机号}」。<br>4. 脱敏规则：手机号中部数字使用 `*` 隐藏，隐藏位数为手机号总位数的二分之一，向下取整。 |
| 更换手机号弹窗 | 第一步－验证码 | 1. 展示「验证码」输入区，左侧为 6 位纯数字验证码输入框，右侧为「获取验证码」按钮。<br>2. 点击「获取验证码」后，向原绑定手机号发送身份验证验证码。<br>3. 按钮进入 60 秒倒计时置灰状态，倒计时期间不可重复点击；倒计时结束后恢复「获取验证码」。<br>4. 验证码有效期为 10 分钟。 |
| 更换手机号弹窗 | 第一步－操作 | 1. 底部横向展示「取消」与「下一步」按钮。<br>2. 点击「取消」：关闭弹窗并清空输入，不修改原手机号。<br>3. 点击「下一步」：校验原手机号验证码。<br>4. 校验成功：进入第二步，清空第一步验证码输入。<br>5. 校验失败：保留当前页面，验证码输入框标红，并展示 Toast「验证码不正确，请重新输入」；验证码过期时提示「验证码已过期，请重新获取」。 |
| 更换手机号弹窗 | 第二步－区号选择 | 1. 进入第二步后，展示步骤说明「绑定新手机号」。<br>2. 新手机号输入区左侧展示区号选择器，默认选中原绑定手机号区号。<br>3. 支持选择：中国大陆 +86、中国香港 +852、新加坡 +65。 |
| 更换手机号弹窗 | 第二步－新手机号输入 | 1. 区号选择器右侧展示新手机号输入框，占位文案为「请输入手机号」。<br>2. 输入框失焦时触发格式校验；校验失败时输入框标红，并在下方展示红字提示「请输入正确的手机号」。<br>3. 格式校验规则：<br>　a. 中国大陆（+86）：11 位纯数字，以 1 开头，第二位为 3-9；<br>　b. 中国香港（+852）：8 位纯数字，以 5、6、8、9 开头；<br>　c. 新加坡（+65）：8 位纯数字，以 8、9 开头。 |
| 更换手机号弹窗 | 第二步－新手机号验证码 | 1. 展示「验证码」输入区，左侧为 6 位纯数字验证码输入框，右侧为「获取验证码」按钮。<br>2. 仅当新手机号通过格式校验后，「获取验证码」按钮可点击；格式不合法时按钮置灰，提示用户「请先输入正确的手机号」。<br>3. 点击后向新手机号发送验证码，按钮进入 60 秒倒计时置灰状态，期间不可重复点击。<br>4. 验证码有效期为 10 分钟。 |
| 更换手机号弹窗 | 第二步－操作 | 1. 底部横向展示「取消」与「确定更换」按钮。<br>2. 点击「取消」：关闭弹窗并清空当前流程输入，不修改原手机号。<br>3. 点击「确定更换」：提交新手机号及验证码进行校验。<br>4. 验证码校验通过：完成手机号更换，关闭弹窗；页面展示绿色 Toast「手机号绑定成功」，持续 1 秒后自动消失；手机号列表信息同步刷新为新手机号的脱敏展示。<br>5. 验证码校验失败：保留当前填写内容，对应验证码输入框标红，并展示具体提示，如「验证码不正确，请重新输入」或「验证码已过期，请重新获取」。 |
| 更换手机号弹窗 | 通用校验与反馈 | 1. 所有验证码仅允许输入数字，最大长度为 6 位。<br>2. 取消、关闭、验证失败均不得修改当前已绑定手机号。<br>3. 页面应防止用户在倒计时期间重复获取验证码。<br>4. 成功 Toast 使用绿色样式，失败 Toast 使用红色样式；成功提示默认展示 1 秒后自动消失。 |
