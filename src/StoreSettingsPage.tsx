import { useState } from 'react'

export function StoreSettingsPage({ onBack, notify, region, onRegionChange }: { onBack: () => void; notify: (message: string) => void; region: string; onRegionChange: (region: string) => void }) {
  const [name, setName] = useState('FKM · 湖滨店')
  const [intro, setIntro] = useState('为顾客提供轻松、舒适的餐饮体验。')
  const [open, setOpen] = useState(false)
  const save = () => { if (!name.trim()) { notify('请填写店铺名称'); return }; notify('保存成功') }
  return <main className="store-settings-page"><header className="header"><button className="icon-button" onClick={onBack}>‹</button><h1>店铺设置</h1><span /></header><section className="store-section"><label>餐厅名称 <em>*</em><input value={name} maxLength={50} onChange={event => setName(event.target.value)} placeholder="请输入店铺名称" /></label><label>餐厅简介 <small>（选填）</small><textarea value={intro} maxLength={200} onChange={event => setIntro(event.target.value)} placeholder="请输入店铺简介" /><span className="store-counter">{intro.length}/200</span></label><div className="store-switch-row"><div><b>营业状态</b><span>{open ? '营业中' : '休息中'}</span></div><button className={`store-switch ${open ? 'on' : ''}`} onClick={() => setOpen(value => !value)}><i /></button></div><label>店铺地区<select value={region} onChange={event => onRegionChange(event.target.value)} data-currency-static><option>新加坡（$）</option><option>中国香港（$）</option><option>中国大陆（¥）</option></select><span className="store-help" data-currency-static>地区决定全局货币符号：新加坡 → $，中国香港 → $，中国大陆 → ¥。</span></label></section><footer className="fixed-action store-save"><button className="primary large" onClick={save}>保存更改</button></footer></main>
}