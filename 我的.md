# 商家端移动端 PRD｜我的

| 页面/一级模块 | 二级模块 | 需求内容 |
| --- | --- | --- |
| 我的 | 页面布局 | 1. 底部导航进入「我的」页面。<br>2. 页面自上而下依次展示：门店信息、语言、提示音、账号绑定信息、登录安全、其他、关于及退出登录。<br>3. 原网页版「账号设置」「偏好设置」内容在移动端直接展开于「我的」页，减少一级跳转；涉及绑定、更换、解绑及密码修改时，使用底部弹窗完成操作。 |
| 我的 | 页面标题 | 顶部居中展示标题「我的」。 |
| 我的 | 门店信息 | 1. 标题下方展示当前门店信息卡。<br>2. 左侧展示门店标识「店」，中部主文案展示店名，例如「FKM · 湖滨店」，辅助文案展示「当前门店」。<br>3. 右侧展示箭头样式入口，为后续门店信息详情预留。 |
| 我的 | 语言 | 1. 分组标题为「语言」。<br>2. 展示「语言设置」及辅助说明「切换系统显示语言」。<br>3. 右侧使用下拉选择框，选项为：简体中文、繁體中文、English。<br>4. 默认选中「简体中文」。<br>5. 用户切换至任一选项后，立即取消原选中项并即时生效；系统全局文本（按钮、菜单、提示等）应切换至对应语言，无需二次确认。<br>6. 切换成功后展示提示：简体中文为「已切换至简体中文」、繁體中文为「已切換至繁體中文」、English 为「Switched to English」。 |
| 我的 | 提示音 | 1. 分组标题为「提示音」。<br>2. 依次展示「订单提示音」「呼叫提示音」两项，每项左侧展示标题、说明及「测试」按钮，右侧展示滑动开关。<br>3. 两个开关默认开启；点击后立即切换启用/禁用状态，无需二次确认。 |
| 我的 | 提示音－订单提示音 | 1. 辅助说明：「新订单进入订单管理时触发提示音」。<br>2. 开启时，用户端/商家端完成下单且订单首次进入商家端订单管理后，触发订单提示音；关闭时不播放提示音。<br>3. 点击「测试」立即播放一次订单提示音，测试行为不受开关状态影响。 |
| 我的 | 提示音－呼叫提示音 | 1. 辅助说明：「新服务请求进入服务请求时触发提示音」。<br>2. 开启时，用户端发起的服务请求首次推送至商家端服务请求列表后，触发呼叫提示音；关闭时不播放提示音，但请求仍正常进入列表。<br>3. AI 来源服务请求进入列表后需要标识为「AI」，提示音是否触发遵循平台推送策略。<br>4. 点击「测试」立即播放一次呼叫提示音，测试行为不受开关状态影响。 |
| 我的 | 账号绑定信息 | 分组标题为「账号设置 · 绑定信息」，依次展示手机号与邮箱。每项均为左侧图标、中部标题及绑定状态、右侧操作按钮的移动端列表布局。 |
| 我的 | 账号绑定信息－手机号状态 | 1. 已绑定：展示标题「手机号」及脱敏号码，格式为「+{区号} {脱敏号码}」；手机号中部隐藏位数为总位数的二分之一（向下取整）。右侧展示「更换」「解绑」。<br>2. 未绑定：展示标题「手机号」及灰色辅助文案「未绑定」；右侧展示「去绑定」。 |
| 我的 | 账号绑定信息－绑定手机号 | 1. 点击「去绑定」后，以底部弹窗展示「绑定手机号」，右上角展示「×」关闭按钮。<br>2. 输入区由区号选择器与手机号输入框组成。区号选项：中国大陆 +86、中国香港 +852、新加坡 +65；默认中国大陆 +86。<br>3. 格式校验：+86 为 11 位纯数字且以 1 开头、第二位为 3-9；+852 为 8 位纯数字且以 5/6/8/9 开头；+65 为 8 位纯数字且以 8/9 开头。失焦校验失败时展示「请输入正确的手机号」。<br>4. 验证码为 6 位数字；仅手机号校验通过后可点击「获取验证码」。点击后进入 60 秒倒计时且不可重复点击。<br>5. 点击「确定」后校验验证码；成功则关闭弹窗、展示 Toast「手机号绑定成功」、刷新脱敏状态；失败时保留弹窗并提示「验证码不正确，请重新输入」或「验证码已过期，请重新获取」。<br>6. 点击「取消」或「×」关闭弹窗并清空本次输入。 |
| 我的 | 账号绑定信息－更换手机号 | 1. 点击「更换」后打开「更换手机号」底部弹窗，分两步完成。<br>2. 第一步「原手机号身份验证」：展示不可编辑的原手机号脱敏信息，输入 6 位验证码后点击「下一步」。验证码正确进入第二步；错误提示「验证码不正确，请重新输入」。<br>3. 第二步「绑定新手机号」：填写区号、新手机号与验证码，格式和验证码规则与绑定手机号一致；点击「确定更换」后完成校验。<br>4. 成功后关闭弹窗、展示 Toast「手机号绑定成功」并刷新手机号展示；失败时保留当前填写内容并展示具体错误。<br>5. 任一步点击「取消」或「×」均退出且不修改原手机号。 |
| 我的 | 账号绑定信息－解绑手机号 | 1. 点击「解绑」后打开「解绑手机号」底部弹窗，展示不可编辑的当前手机号脱敏信息及验证码输入区。<br>2. 获取验证码后进入 60 秒倒计时；点击「确认解绑」后校验验证码。<br>3. 校验成功后关闭弹窗，展示 Toast「手机号解绑成功」，手机号状态切换为「未绑定」，右侧操作变为「去绑定」。<br>4. 校验失败时保留弹窗并展示验证码错误或过期提示；点击「取消」或「×」不修改绑定状态。 |
| 我的 | 账号绑定信息－邮箱状态 | 1. 已绑定：展示标题「邮箱」及脱敏邮箱地址；仅对邮箱前缀中部字符脱敏，隐藏位数为前缀长度的二分之一（向下取整）。右侧展示「更换」「解绑」。<br>2. 未绑定：展示标题「邮箱」及灰色辅助文案「未绑定」；右侧展示「去绑定」。 |
| 我的 | 账号绑定信息－绑定/更换/解绑邮箱 | 1. 绑定、解绑流程分别与手机号绑定、解绑一致，均使用底部弹窗、右上角「×」、6 位验证码、60 秒获取验证码倒计时、取消及成功/失败反馈。<br>2. 更换邮箱需先完成「原邮箱身份验证」，再进入「绑定新邮箱地址」步骤；任一步取消或关闭均不修改原邮箱。<br>3. 邮箱格式要求：符合「用户名@域名.后缀」，支持大小写英文字母、数字、下划线、点号、减号；用户名不超过 64 位，邮箱整体不超过 128 位。校验失败提示「请输入正确的邮箱地址」。<br>4. 绑定或更换成功展示 Toast「邮箱绑定成功」；解绑成功展示 Toast「邮箱解绑成功」并切换为未绑定状态。 |
| 我的 | 登录安全 | 1. 分组标题为「登录安全」。<br>2. 展示「登录密码」及辅助文案「上次修改：YYYY-MM-DD」。<br>3. 右侧展示「修改密码」按钮。 |
| 我的 | 登录安全－修改密码 | 1. 点击「修改密码」后，以底部弹窗展示「修改登录密码」，右上角展示「×」。<br>2. 内容区依次展示原密码、新密码、确认新密码三个密码输入框。<br>3. 新密码下方固定展示规则并实时更新校验标识：① 长度限制：8-20 位字符；② 组合要求：大写字母、小写字母、数字、常用特殊符号（!@#$%^&*_）中，至少包含三类。满足显示绿色对勾，未满足显示灰色空心圆。<br>4. 确认新密码与新密码一致且规则合规时为通过；不一致提示「两次输入的密码不一致」，一致但规则不合规提示「请输入符合要求的新密码」。<br>5. 「确定修改」仅在新密码通过全部规则且两次输入一致时可点击；否则置灰。<br>6. 提交后校验原密码：错误时提示「原密码错误，请重新输入」并标红原密码输入框；成功时关闭弹窗、展示 Toast「密码修改成功」。<br>7. 点击「取消」或「×」关闭弹窗并清空输入内容，不修改密码。 |
| 我的 | 其他 | 分组标题为「其他」，当前展示「帮助与反馈」入口及右侧箭头样式，点击后进入后续帮助与反馈模块；该入口不影响当前账号、偏好或门店数据。 |
| 我的 | 关于 | 分组标题为「关于」，展示「当前版本」及版本号，例如「v0.1.0」。版本号为只读信息。 |
| 我的 | 退出登录 | 1. 页面底部展示红色弱强调按钮「退出登录」。<br>2. 点击后弹出二次确认弹窗，标题为「确认退出登录」，提示文案为「确定要退出当前账号吗？」。<br>3. 点击「取消」关闭弹窗并保留当前登录状态；点击「确认」清除当前登录会话并返回登录页。 |
| 我的 | 通用反馈 | 1. 成功操作使用绿色 Toast，默认展示 1 秒后自动消失。<br>2. 校验失败时对应输入项展示红色错误提示，且不关闭当前底部弹窗。<br>3. 底部弹窗展示半透明遮罩，遮罩期间不可操作页面背景。 |
