# 商家端移动端 PRD｜服务请求

| 页面/一级模块 | 二级模块 | 需求内容 |
| --- | --- | --- |
| 服务请求 | 页面定位 | 供门店人员集中查看、确认处理及清理服务请求。服务请求来源支持「用户」与「AI」两类；页面不提供日期筛选，默认聚焦当前待处理事项。 |
| 服务请求 | 页面标题 | 顶部居中展示页面标题「服务请求」。 |
| 服务请求 | 顶部批量操作 | 标题下方横向展示「✓ 全部处理」与「删除已处理」两个按钮。按钮均覆盖完整可点击区域。 |
| 服务请求 | 全部处理 | 1. 默认展示格式为「✓ 全部处理（X）」，其中 X 为当前待处理请求数量；无待处理请求时不展示数量且按钮置灰。<br>2. 点击后，将当前全部「待处理」请求批量更新为「已处理」。<br>3. 更新成功后，列表、各筛选结果及数量实时刷新，并展示 Toast「已处理 X 条服务请求」。 |
| 服务请求 | 删除已处理 | 1. 有至少一条已处理请求时按钮可点击；无已处理请求时置灰。<br>2. 点击后弹出二次确认弹窗，文案为「确定删除 X 条已处理服务请求吗？」。<br>3. 点击「取消」关闭弹窗，不修改数据；点击「确认」后删除全部已处理请求，列表与数量实时刷新，并展示 Toast「已删除已处理服务请求」。<br>4. 删除操作仅作用于已处理请求，不影响待处理请求。 |
| 服务请求 | 状态筛选 | 1. 顶部批量操作下方展示筛选项：「全部」「待处理」「已处理」。<br>2. 默认选中「待处理」。<br>3. 点击筛选项后立即刷新下方列表，仅展示对应状态的服务请求；「全部」展示所有状态。<br>4. 当前选中项以蓝色高亮展示，未选中项以浅灰底展示。 |
| 服务请求 | 列表标题与数量 | 筛选项下方左侧展示「请求列表」，右侧展示当前筛选结果数量，格式为「共 X 条」。批量处理、删除或单条处理后，数量实时更新。 |
| 服务请求 | 请求列表 | 1. 服务请求以纵向卡片列表展示，按请求创建时间倒序排列，最新请求置顶。<br>2. 卡片之间保持间距，页面可纵向滚动查看全部内容。<br>3. 不设置卡片右上角展开/收起三角入口。 |
| 服务请求 | 单条请求－主信息 | 卡片首行左侧展示主标题，格式为「{桌台编号} 桌 · {请求来源}」。示例：「A02 桌 · 用户」「B01 桌 · AI」。<br>请求来源仅支持：用户、AI。 |
| 服务请求 | 单条请求－请求时间 | 主标题下方展示请求时间，格式为「MM-DD HH:mm」，示例：「08-16 12:26」。时间为请求创建时间，前端只读展示。 |
| 服务请求 | 单条请求－请求内容 | 卡片下半部分以分割线与主信息区区隔，展示具体请求内容。例如「麻烦帮忙加两杯温水，谢谢。」或 AI 识别后生成的服务建议。内容支持自动换行完整展示。 |
| 服务请求 | 单条请求－确认处理 | 1. 状态为「待处理」时，卡片右侧展示主操作按钮「✓ 确认处理」。<br>2. 点击后，该请求状态立即更新为「已处理」，按钮替换为不可点击的「已处理」状态标识，并展示 Toast「服务请求已处理」。<br>3. 已处理状态使用绿色弱强调样式，避免与可操作的确认处理按钮混淆。 |
| 服务请求 | 空状态 | 当当前筛选条件下无数据时，页面展示空状态图标、对应提示文案及辅助说明：<br>1. 待处理：`暂无待处理服务请求`；<br>2. 已处理：`暂无已处理服务请求`；<br>3. 全部：`暂无服务请求`；<br>辅助说明统一为「新的服务请求会在这里显示。」 |
| 服务请求 | 数据刷新与异常 | 1. 首次进入、手动刷新或接口重新请求时展示加载状态，避免展示旧的错误数量。<br>2. 请求失败时展示失败提示「服务请求加载失败，请检查网络后重试」及「重新加载」按钮。<br>3. 批量处理、单条处理、删除失败时保留原列表状态，并展示对应失败 Toast。 |
| 我的 | 偏好设置－呼叫提示音 | 1. 「我的」页面中的「提示音」区展示「呼叫提示音」开关及「测试」入口；默认开启。<br>2. 开启时，新的服务请求进入服务请求列表后触发呼叫提示音；关闭时不播放提示音，但请求仍正常进入列表及待处理统计。<br>3. 点击「测试」立即播放一次呼叫提示音，且不受开关状态影响，用于试听与校验。<br>4. 用户端发起服务请求并首次推送至商家端时，判定为新呼叫；AI 来源请求按平台推送规则进入列表并标注来源为「AI」。 |
