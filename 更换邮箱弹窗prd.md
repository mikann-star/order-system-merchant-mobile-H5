# 更换邮箱弹窗 PRD

| 页面/一级模块 | 二级模块 | 需求内容 |
| ------- | ---------- | ---- |
| 我的 | 账号绑定信息－邮箱 | 1. 邮箱处于已绑定状态时，右侧展示「更换」主按钮与「解绑」次按钮。<br>2. 点击「更换」按钮后，打开「更换邮箱」底部弹窗。<br>3. 弹窗从页面底部弹出，顶部圆角；弹窗外展示半透明遮罩。<br>4. 弹窗展示期间不可操作背景页面。<br>5. 弹窗整体采用顶部栏、内容区、底部操作区的三段式结构。 |
| 更换邮箱弹窗 | 顶部栏 | 1. 左侧展示标题「更换邮箱」。<br>2. 右侧展示「×」关闭按钮。<br>　1. 点击「×」后关闭弹窗，清空当前流程中已输入的验证码、新邮箱地址等信息，不修改原绑定邮箱。 |
| 更换邮箱弹窗 | 流程规则 | 1. 更换流程分为两步：<br>　1. 「原邮箱身份验证」；<br>　2. 「绑定新邮箱地址」。<br>2. 用户必须完成第一步验证后，才可进入第二步。关闭弹窗或点击任一步「取消」均终止流程，原邮箱保持不变。 |
| 更换邮箱弹窗 | 第一步－内容区 | 1. 步骤说明：「验证原邮箱身份」。<br>2. 当前邮箱：<br>　1. 展示当前绑定邮箱脱敏信息，字段只读不可编辑。<br>　2. 脱敏规则：对邮箱前缀中部字符使用 `*` 隐藏，隐藏位数为邮箱前缀字符数的二分之一，向下取整；域名及后缀保持完整展示。<br>3. 验证码：<br>　1. 「验证码」输入区：<br>　　1. 占位文案：请输入6位验证码。<br>　　2. 「获取验证码」按钮：<br>　　　1. 点击后，按钮进入60秒倒计时置灰状态，期间不可重复点击；后端向原绑定邮箱发送身份验证邮件。<br>　　　2. 邮件主题：【FKM 点餐系统】邮箱更换身份验证。<br>　　　3. 邮件正文：您好，您正在进行邮箱更换操作，本次身份验证验证码为 **{6位数字验证码}**。验证码有效期为10分钟，请勿将验证码泄露给他人。如非本人操作，请立即登录账户更换密码。—— FKM 点餐系统团队。 |
| 更换邮箱弹窗 | 第一步－操作区 | 底部横向展示「取消」与「下一步」按钮。<br>1. 点击「取消」：关闭弹窗并清空输入，不修改原邮箱。<br>2. 点击「下一步」：提交原邮箱验证码校验。<br>　1. 校验成功：进入第二步。<br>　2. 校验失败：<br>　　1. 验证码输入框标红。<br>　　2. Toast 提示「验证码不正确，请重新输入」/「验证码已过期，请重新获取」，持续1秒后消失，不关闭弹窗。 |
| 更换邮箱弹窗 | 第二步－内容区 | 1. 步骤说明：「绑定新邮箱地址」。<br>2. 新邮箱地址输入区：<br>　1. 输入框标题：「新邮箱地址」。<br>　2. 占位文案：请输入邮箱地址。<br>　3. 邮箱格式校验规则：需符合「用户名@域名.后缀」标准邮箱规范；支持大小写英文字母、数字、下划线、点号、减号；用户名长度不超过64位，整体邮箱地址长度不超过128位。<br>　4. 输入失焦时触发格式校验；校验不通过时输入框标红，下方以红色小字提示「请输入正确的邮箱地址」。<br>3. 新邮箱验证码输入区：<br>　1. 左侧为6位数字验证码输入框，右侧为「获取验证码」按钮。<br>　2. 获取验证码前必须先通过邮箱格式校验；格式非法时点击「获取验证码」按钮，Toast 提示「请先输入正确的邮箱地址」，持续1秒后自动消失。<br>　3. 点击「获取验证码」后，按钮进入60秒倒计时置灰状态，后端向新邮箱发送验证邮件。<br>　　1. 邮件主题：【FKM 点餐系统】邮箱绑定验证码。<br>　　2. 邮件正文：您好，您正在进行邮箱绑定操作，本次验证码为 **{6位数字验证码}**。验证码有效期为10分钟，请勿将验证码泄露给他人。如非本人操作，请忽略本邮件。—— FKM 点餐系统团队。 |
| 更换邮箱弹窗 | 第二步－操作区 | 左侧为「取消」按钮，右侧为「确定更换」主按钮。<br>1. 点击「确定更换」按钮，后端校验验证码有效性。<br>　1. 校验通过：弹窗关闭，展示绿色 Toast 提示「邮箱更换成功」，持续1秒后自动消失；同步刷新页面邮箱绑定状态及按钮展示内容。<br>　2. 校验失败：对应验证码输入框标红，Toast 提示具体原因（如「验证码不正确，请重新输入」/「验证码已过期，请重新获取」），不关闭弹窗。<br>2. 点击「取消」按钮：关闭弹窗，不进行邮箱更换操作。 |
