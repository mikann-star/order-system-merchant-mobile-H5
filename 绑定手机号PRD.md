# 商家端移动端－绑定手机号 PRD

| 页面/一级模块 | 二级模块 | 需求内容 |
| ------- | ---- | ---- |
| 我的 | 账户设置－手机号状态 | 1. 本功能前置条件为当前账号未绑定手机号。<br>2. 未绑定状态下，信息行左侧展示手机图标；中间从上至下展示标题「手机号」与灰色辅助文案「未绑定」；右侧展示「去绑定」按钮。<br>3. 点击「去绑定」进入「绑定手机号」弹窗。 |
| 绑定手机号弹窗 | 弹窗结构 | 1. 弹窗以底部弹层形式展示，背景展示半透明遮罩；遮罩展示期间不可操作页面背景内容。<br>2. 弹窗采用三段式结构：顶部栏、内容区、底部操作栏。<br>3. 顶部栏左侧展示标题「绑定手机号」，右侧展示「×」关闭按钮。<br>4. 点击「×」关闭弹窗，清空本次输入内容，不执行绑定操作。 |
| 绑定手机号弹窗 | 手机号输入区 | 1. 内容区首先展示「手机号」字段。<br>2. 输入区由左侧区号选择器与右侧手机号输入框组成。<br>3. 区号默认选中「中国大陆 +86」，支持切换：中国大陆 +86、中国香港 +852、新加坡 +65。<br>4. 手机号输入框仅允许输入数字，占位文案为「请输入手机号」。<br>5. 切换区号后，保留已输入号码并按新地区规则重新校验。 |
| 绑定手机号弹窗 | 手机号格式校验 | 1. 输入框失焦、点击获取验证码或点击确定时触发校验。<br>2. 中国大陆 +86：仅支持 11 位数字，首位为 1，第二位为 3－9。<br>3. 中国香港 +852：仅支持 8 位数字，首位为 5、6、8 或 9。<br>4. 新加坡 +65：仅支持 8 位数字，首位为 8 或 9。<br>5. 校验失败时输入框标红，并在字段下方展示红色提示「请输入正确的手机号」。 |
| 绑定手机号弹窗 | 短信验证码输入区 | 1. 手机号输入区下方展示「验证码」字段。<br>2. 左侧为 6 位纯数字验证码输入框，占位文案为「请输入 6 位验证码」；右侧为「获取验证码」按钮。<br>3. 手机号格式未通过校验时，「获取验证码」按钮置灰且不可点击。<br>4. 手机号格式通过后，点击「获取验证码」向当前区号与手机号对应的号码发送绑定验证码。<br>5. 发送成功后按钮进入 60 秒倒计时，展示格式为「{N}s 后重试」，倒计时期间置灰且不可重复点击。<br>6. 验证码有效期为 10 分钟；当前移动端原型使用演示验证码「123456」完成校验。 |
| 绑定手机号弹窗 | 验证码短信 | 1. 短信内容为：【FKM 点餐系统】您正在进行手机号绑定操作，验证码为 {6 位数字验证码}，10 分钟内有效，请勿泄露给他人。如非本人操作请忽略。<br>2. 验证码发送目标为用户当前选择区号与填写手机号组合后的完整号码。 |
| 绑定手机号弹窗 | 操作栏 | 1. 底部横向展示「取消」与「确定」按钮。<br>2. 点击「取消」：关闭弹窗，清空已输入手机号、验证码与校验提示，不执行绑定。<br>3. 点击「确定」：依次校验手机号格式与验证码有效性；校验通过后提交绑定请求。 |
| 绑定手机号弹窗 | 提交校验与反馈 | 1. 手机号格式不合法时：不提交绑定请求，手机号输入框标红，展示提示「请输入正确的手机号」。<br>2. 验证码为空、非 6 位数字或校验失败时：不关闭弹窗，验证码输入框标红，展示提示「验证码不正确，请重新输入」。<br>3. 验证码过期时：不关闭弹窗，展示提示「验证码已过期，请重新获取」。<br>4. 绑定成功时：关闭弹窗，展示绿色 Toast「手机号绑定成功」，持续 1 秒后自动消失。 |
| 我的 | 绑定成功后的数据联动 | 1. 绑定成功后，账户设置中的手机号信息实时更新为已绑定状态。<br>2. 左侧保留手机图标；中间从上至下展示「手机号」与脱敏号码；右侧展示「更换」主按钮和「解绑」次按钮。<br>3. 脱敏展示格式为「+{区号} {脱敏手机号}」；隐藏手机号中部数字，隐藏位数为号码总位数的二分之一并向下取整。 |
| 绑定手机号弹窗 | 异常与兜底 | 1. 网络异常、验证码发送失败或绑定接口失败时，不关闭弹窗，保留用户已输入内容，并展示对应失败提示。<br>2. 用户关闭弹窗后再次进入绑定流程时，不保留上一次未提交的手机号、验证码与倒计时状态。 |
